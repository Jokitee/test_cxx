# RoboMaster 自瞄决策规划器（Fire Control Planner）设计文档

## 0. 文档定位

本文档描述自瞄系统中「预测之后、发弹之前」的决策规划模块（Planner），衔接：

```
装甲板检测 → EKF 状态估计/预测（已完成）→ 【本文档：规划器】 → 发弹指令
```

规划器职责：**在多目标、多装甲板、动态弹道的条件下，决定打谁、打哪块板、云台往哪转**，核心设计目标为——

> **使用者在任意时刻按下扳机，只要此刻自瞄已锁定目标，都应命中。**

这意味着开火时机不再由规划器自主判断，而是由人随时触发；规划器的责任是保证**云台在每一帧输出的角度，都是"假设此刻开火"条件下的正确解**，而不是等待一个"最佳时机"再瞄准。这是一个从"自主开火决策"到"持续正确性保证"的设计哲学转变，会影响本文档第 4、6 节的具体实现。

---

## 1. 模块整体架构

```
┌─────────────────────────────────────────────────────────┐
│                     Fire Control Planner                  │
│                                                             │
│  ┌───────────┐   ┌───────────┐   ┌───────────┐           │
│  │ 目标筛选   │ → │ 目标评分   │ → │ 目标选择   │           │
│  │ Filter    │   │ Scoring   │   │ Selection │           │
│  └───────────┘   └───────────┘   └───────────┘           │
│                                        │                    │
│                                        ▼                    │
│  ┌───────────┐   ┌───────────┐   ┌───────────┐           │
│  │ 装甲板选择 │ → │ 弹道解算   │ → │ 开火决策   │           │
│  │ Armor Sel │   │ Ballistics│   │ Fire Dec. │           │
│  └───────────┘   └───────────┘   └───────────┘           │
│                                        │                    │
└────────────────────────────────────────┼────────────────────┘
                                          ▼
                              云台角度指令 + 开火信号
```

### 输入
- 多目标 EKF 状态列表（位置、速度、角速度、每块装甲板法向量、协方差）
- **模型构建状态标志**（见下方 1.1，标明当前输入是"完整模型"还是"粗略兜底模型"输出）
- 己方云台当前角度、弹速（裁判系统）、剩余热量/弹量
- 己方血量、比赛剩余时间（可选，用于风险策略调整）

### 1.1 输入层模型状态管理（完整模型 / 粗略兜底）

完整的小陀螺模型（估计旋转半径、角速度、装甲板切换关系等）需要**足够多的观测帧**才能收敛，在目标刚出现、频繁遮挡、或旋转模式突变（比如从平动切到陀螺）时会有一段"模型未构建成功"的空窗期。这段时间不能没有输出，否则云台会呆滞或乱跳，因此规划器的输入层需要显式区分三种模型状态，并对应不同粒度的估计：

| 模型状态 | 触发条件 | 输入层提供的数据 |
|---|---|---|
| `MODEL_NOT_BUILT` | 目标刚出现 / 刚重新捕获，观测帧数 < 最小建模帧数（如 5~10 帧） | 仅当前帧原始装甲板位姿，无速度/角速度估计 |
| `MODEL_ROUGH`（粗略 EKF+预测） | 观测帧数够用一个简化模型收敛，但不足以支撑完整小陀螺模型（旋转半径、多板切换关系等未收敛） | **单装甲板匀速/匀加速 EKF**（CV/CA 模型）给出的位置+速度估计，不含旋转半径与多板切换预测 |
| `MODEL_FULL` | 完整模型（含旋转半径、角速度、多板关联）已收敛，协方差达标 | 完整状态向量，供第 4.2 节的多板协同选择使用 |

**粗略 EKF 的最小实现**：状态向量退化为 `[x, y, z, vx, vy, vz]`（或再加 `ax, ay, az`），只对"当前正在跟踪的这一块装甲板"做常规匀速/匀加速滤波，不引入旋转中心、半径等强假设参数——这些参数在观测不足时反而会因为过拟合噪声而发散，粗略模型的目的正是"少假设、少参数、快收敛"。

```python
class RoughEKF:
    """
    退化版 EKF：仅用于 MODEL_ROUGH 状态，状态量 [x,y,z,vx,vy,vz]
    不引入角速度/半径等小陀螺专属参数，避免观测不足时发散
    """
    def __init__(self):
        self.state = None      # [x,y,z,vx,vy,vz]
        self.P = None          # 协方差
        self.min_frames = 5    # 至少积累这么多帧才认为粗略模型可用

    def update(self, armor_pos, dt):
        # 标准 CV 模型预测 + 观测更新（略）
        ...

    def is_usable(self):
        # 协方差收敛到一定程度即可判定粗略模型可用，门槛低于 MODEL_FULL
        return self.frame_count >= self.min_frames and trace(self.P) < rough_threshold
```

**状态间的切换与降级原则**：
- `MODEL_NOT_BUILT → MODEL_ROUGH`：一旦粗略 EKF 收敛即可切换，门槛应设置得**远低于**完整模型，保证尽快有可用输出
- `MODEL_ROUGH → MODEL_FULL`：观测帧数足够、且完整模型协方差也收敛后再切换，不能仓促切换导致状态跳变
- `MODEL_FULL → MODEL_ROUGH`（降级）：完整模型跟丢/协方差发散（比如目标突然被遮挡后重新出现、运动模式突变）时，**优先降级到粗略模型继续输出，而不是直接进入 MODEL_NOT_BUILT**，因为粗略模型收敛更快、能更快恢复有效瞄准
- 任何状态切换都要配合第 6.5 节的平滑约束，避免云台角度因为模型内部切换而产生肉眼可见的跳变

**下游模块的相应处理**：
- 第 4 节装甲板选择：`MODEL_ROUGH` 状态下没有角速度/半径信息，无法做"预测飞行时间后该板朝向"的计算，此时退化为直接用粗略 EKF 给出的当前预测位置解算弹道，即按 4.1 节"静止/慢速目标"的逻辑处理，不强行套用小陀螺公式
- 第 6 节锁定置信度：`MODEL_ROUGH` 状态上限设为 TRACKING，不允许进入 LOCKED（即使角度误差很小），因为粗略模型对多板切换、遮挡等场景鲁棒性不足，误判命中率的风险更高；只有 `MODEL_FULL` 才能进入 LOCKED

### 输出
- **云台目标角度（yaw, pitch）** —— 唯一的核心输出，每帧持续更新，代表"此刻开火即可命中"的解算结果
- 目标车辆 ID / 目标装甲板 ID（用于日志、可视化、UI 提示，不是开火前提）
- 锁定置信度（0~1 或离散等级）—— 供 UI 显示"是否已锁定"，供使用者判断是否此刻可开火

> 注意：本设计不再输出"开火使能信号"作为强制门槛。开火权完全交给使用者（扣扳机），规划器只负责让当前角度始终有效。置信度可以用于 UI 提示（如准星变色），但不应该拦截使用者的开火动作——这是人机权责的边界。

---

## 2. 目标筛选（Filter）

先剔除不该考虑的目标，减少后续计算量并避免误判：

| 筛选条件 | 说明 |
|---|---|
| 跟踪丢失超时 | EKF 连续未更新帧数 > 阈值（如 10 帧）→ 剔除 |
| 协方差过大 | 位置/速度协方差超过阈值 → 视为不可靠，暂不进入评分 |
| 超出弹道有效射程 | 距离超过标定的最大有效命中距离 → 剔除 |
| 装甲板法向量背对己方 | 所有可见装甲板法向量与视线夹角 > 90° → 该车暂不可打 |
| 己方射界限制 | 云台机械限位/友军遮挡范围内 → 剔除 |

> 注：处于 `MODEL_NOT_BUILT` 状态的目标（观测帧数不足，见 1.1 节）不进入评分，但也不应被当作"丢失"处理——只是暂不参与目标选择，规划器应继续显示/跟踪，等粗略模型收敛后自动纳入候选。

---

## 3. 目标评分（Scoring）

对通过筛选的每个目标车辆计算综合得分，公式：

```
Score(target) = w_dist · S_dist + w_angle · S_angle
              + w_conf · S_conf + w_threat · S_threat − Penalty_switch
```

### 3.1 各子项定义

| 子项 | 计算方式 | 说明 |
|---|---|---|
| S_dist | `1 / (distance + ε)`，归一化到 [0,1] | 越近分越高，命中率高、预测误差小 |
| S_angle | `1 − armor_angle / 90°` | 装甲板越正对我方分越高 |
| S_conf | EKF 收敛程度（如 trace(P) 的倒数归一化） | 跟踪越稳定越优先，避免打"半生不熟"目标 |
| S_threat | 按战术自定义（如目标是否正在瞄准我方、目标类型英雄/步兵/哨兵权重） | 可选项，视战术需求开启 |

### 3.2 切换惩罚（Penalty_switch）

```
Penalty_switch = λ, 若 target_id != last_target_id 且 last_target_score_now 未显著更差
                = 0,  若延续上一帧目标
```

**目的**：防止云台在两个高分目标间高频抖动切换，导致"哪个都追不上"。经验做法：只有新目标得分超过当前目标得分一定比例（如 20%）才允许切换。

### 3.3 权重建议（需实测调参）

```
w_dist = 0.4, w_angle = 0.35, w_conf = 0.2, w_threat = 0.05
```
（起始值，需结合场地/弹速/己方战术在实战数据上标定）

---

## 4. 装甲板选择（同车四板协同）

选定目标车辆后，在其可见装甲板中选出最优打击板。

### 4.1 静止/慢速目标
直接选 `armor_angle` 最小（最正对）的装甲板。

### 4.2 小陀螺目标（核心场景，锁定 + 阈值切换的衔接式选择）

因为使用者可能在任意瞬间扣扳机，装甲板的瞄准点必须始终符合"假设现在开火即可命中"的原则（沿用第 6.3 节的计算方式）。但**"每帧独立重新扫描所有板选最优"的做法容易在两块板角度接近时来回抖动**，也没有考虑一个更重要的问题：**装甲板角度越倾斜，弹丸命中后的有效动量/伤害越低（容易跳弹或斜切），不是"进入容忍角度"就该打，而是要在合适的角度区间内稳定停留，并在目标即将转出该区间前，提前衔接到下一块正在转入的装甲板**，避免出现"当前板已经太斜、下一块还没转到位"的攻击真空期。

设计为**锁定 + 阈值触发切换**的逻辑：

```python
def select_armor_with_handoff(armors, current_selected_id, angular_velocity,
                                switch_threshold, min_effective_angle):
    """
    armors: 当前可见装甲板列表，每个含 current_angle（相对法向角，正对为0）
    current_selected_id: 当前锁定的装甲板 index（None 表示尚未锁定任何板）
    switch_threshold: 触发切换的角度阈值——当前板角度的绝对值超过此值，
                       视为"即将过于倾斜"，应开始衔接下一块板
    min_effective_angle: 允许开火的最大倾斜角度（硬性上限，超过此值动量不足/易跳弹，
                          不应作为目标，哪怕暂时没有更好的候选）

    返回: 新的锁定装甲板 index
    """
    # 1. 若当前锁定板仍在“稳定可打”区间内，继续锁定，不切换
    if current_selected_id is not None:
        cur_angle = abs(armors[current_selected_id].current_angle)
        if cur_angle < switch_threshold:
            return current_selected_id

    # 2. 当前板已经越过切换阈值（或尚未锁定任何板）：
    #    在候选板中，选择“正在转入正对方向”（角度绝对值随时间减小）
    #    且已经进入有效角度范围内的那一块，实现无缝衔接
    candidates = []
    for i, armor in enumerate(armors):
        angle = armor.current_angle
        # 用角速度方向判断该板是否正在转向 0 度（正对），而不是正在转离
        approaching = (angle * angular_velocity) < 0
        if approaching and abs(angle) <= min_effective_angle:
            candidates.append(i)

    if candidates:
        # 选角度绝对值最小（最接近正对）的候选，作为新的锁定目标
        return min(candidates, key=lambda i: abs(armors[i].current_angle))

    # 3. 没有板满足衔接条件（比如目标刚好处于两块板都很斜的过渡区间）：
    #    保留当前锁定，即使它已经超过 switch_threshold，只要还没超过硬上限
    if current_selected_id is not None and abs(armors[current_selected_id].current_angle) <= min_effective_angle:
        return current_selected_id

    return None  # 确实没有可用板，交给第6节 TRACKING 状态处理
```

**策略要点**：
- 用**双阈值（磁滞）**替代单一容忍窗口：`switch_threshold`（较小，触发"该找下家了"）+ `min_effective_angle`（较大，硬性上限，超过就不该再打这块板），中间这段区间用于平滑过渡，避免板刚好卡在临界角度附近时来回切换
- 切换判断依据角速度方向：只衔接"正在转向正对方向"的板，不会误选正在转远的板，保证衔接后还能稳定停留一段时间，而不是衔接完立刻又要切
- `min_effective_angle` 直接体现"避免攻击装甲板过于倾斜导致动量不够"的要求——这是一条硬约束，不是评分项，超过此角度的板不应被选中，哪怕当前没有更好的候选（此时应交给第6节的 TRACKING/降级逻辑，而不是勉强瞄一个无效角度）
- 每次锁定的板确定后，具体瞄准点仍按第 6.3 节"假设现在开火"的原则实时计算，本节只负责"打哪一块板"，不影响"这一刻瞄准哪个点"
- 与第 6 节联动：连续锁定同一块板超过阈值区间时才算稳定（可支撑 LOCKED 状态），刚发生衔接切换的瞬间，建议给 1~2 帧的置信度过渡（不要立刻跳到 LOCKED），防止衔接瞬间恰好被使用者开火但角度还没完全收敛

---

## 5. 弹道解算（Ballistics）

见附录 A（与预测模块的迭代耦合算法）。核心输出：

- `pitch`：解算后的俯仰角
- `yaw`：`atan2(x, z)` 直接计算（重力不影响水平方向）
- `fly_time`：飞行时间，回馈给 EKF 用于位置预测

**与目标预测的耦合**：
1. 用初始飞行时间猜测值（如 0.15s）预测目标未来位置
2. 用该预测位置解算弹道，得到新的飞行时间
3. 重复 2 次迭代即可收敛（弹速稳定情况下无需更多次）

---

## 6. 持续瞄准正确性保证（Continuous Aim Guarantee）

> 本节替代传统"开火决策"模块。由于开火由使用者随时触发，系统不做"是否开火"的判断，只做"当前角度是否有效"的判断，并把结果体现在**锁定置信度**上，而不是拦截开火动作。

### 6.1 每帧核心循环

```
状态: SEARCHING → TRACKING → LOCKED
```

| 状态 | 进入条件 | 云台角度输出行为 |
|---|---|---|
| SEARCHING | 无可用目标 | 保持当前角度或按预设巡逻逻辑，不输出解算角度 |
| TRACKING | 有目标但协方差未收敛 / 无装甲板满足容忍窗口 | 输出"当前最优估计角度"（哪怕置信度不高），持续逼近有效解，绝不能因为"还没收敛"就停止更新或跳变 |
| LOCKED | 协方差收敛 且 存在满足 6.2 节动态阈值的装甲板解 | 输出精确解算角度，置信度设为高，UI 可提示"已锁定" |

**关键原则**：无论处于哪个状态，云台角度都必须**连续、平滑、始终朝着"当前最合理的开火目标"输出**——因为使用者看不到系统内部状态机，只看得到准星位置，如果准星突然冻结或跳变，恰好在这一帧开火就会脱靶。TRACKING 状态下也要给出角度，只是精度置信度较低。

### 6.2 角度容忍阈值动态调整

角度误差阈值不是开火的"门槛"（门槛已交给人），而是用于判定 LOCKED / TRACKING 状态、驱动 UI 反馈的依据，仍应按距离衰减：

```
threshold(distance) = base_threshold × (min_distance / distance)
```
距离越远，能被判定为"已锁定"的角度误差要求越严格，因为同样角度误差在远处对应更大的实际偏移。

### 6.3 弹道飞行时间的"假设立即开火"原则

每一帧解算飞行时间时，都应假设**使用者会在下一帧扣下扳机**，即：

```
fly_time = solve_from(current_gimbal_pose, target_predicted_pos_at(t = now))
```

而不是假设某个"未来最佳开火时刻"。这样才能保证任意帧输出的角度对应的都是"现在开枪"的正确解，而非"未来某时刻开枪"的解——否则会出现"提前量算多了，人一按就打空"的问题。

### 6.4 目标/装甲板短暂丢失的兜底

- 短时间丢失（如 < 5 帧）：用 EKF 做纯预测外推，继续输出角度，不冻结、不清空
- 长时间丢失：回到 SEARCHING，但角度应平滑衰减到最后已知方向，而不是瞬间归零/复位，避免使用者手动补枪时被系统抢夺瞄准点

### 6.5 平滑约束

角度输出建议加一阶低通滤波或限制最大角速度变化率，防止因为检测抖动导致准星肉眼可见的"抽搐"，这类抽搐会让使用者无法判断"现在按是否会命中"。

---

## 7. "随时开火都命中"策略要点小结

1. **开火权交给人，正确性责任交给系统**：规划器不判断"该不该开火"，只保证"此刻若开火，角度是否正确"
2. **假设立即开火来解算，而非等待未来时机**：飞行时间、装甲板选择都基于"现在开枪"计算，杜绝提前量过量导致的脱靶
3. **角度输出连续平滑，绝不冻结或跳变**：哪怕置信度低也要给出当前最优估计，因为使用者随时可能扣扳机
4. **锁定置信度只做 UI 反馈，不做拦截**：用置信度/准星变色告诉使用者"现在打命中率高不高"，但不剥夺开火权
5. **短暂丢失要外推，长时间丢失要平滑衰减**：避免云台在丢失目标瞬间乱转，干扰使用者手动瞄准
6. **热量/弹量仍是硬约束**：可以在 UI 层提示，但角度解算不受影响，让使用者自行决策是否开火

---

## 附录 A：弹道迭代解算伪代码

```cpp
Eigen::Vector3d target_pos = ekf.getCurrentState();
double fly_time = 0.15; // 初始猜测

for (int i = 0; i < 2; ++i) {
    Eigen::Vector3d predicted_pos = ekf.predict(fly_time);
    double dist_xy = sqrt(predicted_pos.x()*predicted_pos.x()
                         + predicted_pos.z()*predicted_pos.z());
    double height = predicted_pos.y();

    double pitch, t;
    solvePitch(dist_xy, height, ballistic_params, pitch, t);
    fly_time = t;
}

double yaw = atan2(predicted_pos.x(), predicted_pos.z());
```

## 附录 B：待标定/调试参数清单

| 参数 | 说明 | 标定方式 |
|---|---|---|
| k（空气阻力系数） | 弹道模型精度关键参数 | 多距离打靶反推/曲线拟合 |
| 开火角度阈值 base_threshold | 影响命中率与开火频率的权衡 | 场地实测统计命中率 |
| 目标评分权重 w_* | 影响目标选择合理性 | 录像复盘 + 实战调参 |
| 切换惩罚 λ | 影响云台稳定性 | 观察是否出现抖动切换现象 |
| 稳定帧数 N | 影响开火响应速度与误触发率 | 结合相机帧率调整 |
| 最小建模帧数 min_frames | 影响 MODEL_ROUGH 切入速度，过小易发散、过大则空窗期长 | 实测粗略 EKF 在不同帧数下的收敛稳定性 |
| rough_threshold / full_threshold | 粗略/完整模型的协方差收敛判定门槛 | 结合第 6.2 节 LOCKED 状态实测调整 |
| switch_threshold（装甲板切换阈值） | 当前板角度超过此值即开始寻找下一块衔接板，过小则频繁切换，过大则容易打到过斜的板 | 结合命中率与跳弹/斜切统计实测 |
| min_effective_angle（有效命中角度上限） | 超过此倾斜角度视为无效目标（动量不足/易跳弹），硬性上限 | 结合弹丸/装甲板材质做斜入射命中测试 |

---

## 附录 C：后续可扩展方向

- 加入队友雷达站/哨兵视角的目标信息融合，提升多目标决策全局性
- 引入目标"威胁模型"（如目标云台朝向预测，判断是否正瞄准我方）
- 打符场景需要单独的角度-时间预测模型，与本文档的车辆自瞄逻辑分开维护
